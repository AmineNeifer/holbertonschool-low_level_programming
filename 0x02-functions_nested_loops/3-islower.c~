#include "holberton.h"
/**
 * _islower - checks if the charcater is upper case or not
 * int c ;
 * Return: Always 0.
 */
int _islower(int c)
{
if ((ch >= 'a') && (ch <= 'z'))
{
return (1);
}
else if ((ch >= 'A') && (ch <= 'Z'))
{
return (0);
}
}
