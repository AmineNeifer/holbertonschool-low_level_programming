#include "holberton.h"
/**
 * _abs - prints the absolute value of an integer 
 * @r: int type
 * Return: r if it is positive and -r if it is negative
 */
int _abs(int r)
{
int r;
if (r < 0)
{
return(-r);
}
else
return (r);
}
